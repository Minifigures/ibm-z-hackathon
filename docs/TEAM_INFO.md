# Team Info

Use this page for demo-day handoff so judges and teammates know who owns what.

## Members

- Name:
  - Role:
  - Contact:
  - Responsibilities:

## Runtime Ownership

- Backend API:
- Frontend:
- Demo operator:
- Backup operator:

## Demo Credentials Checklist

- [ ] `WATSONX_APIKEY` set locally in `backend/.env`
- [ ] `WATSONX_PROJECT_ID` set locally in `backend/.env`
- [ ] `INVOKE_BASE_URL` set (optional fallback)
- [ ] `ANTHROPIC_API_KEY` set (optional fallback)
